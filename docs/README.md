# docs

Put the demo recording here as `demo.gif` (or `demo.png` for a still). The
top-level README references `docs/demo.gif`.

Keep in mind that anything placed here is published with the repository, so
check the frame for email addresses, document titles and document IDs before
committing a recording.
